/* ============================================================
   Brand.strings — Rooster-specific overrides.
   This table is looked up BEFORE Shared.strings, so any key defined
   here overrides the shared copy for the Rooster brand only.
   Leave empty when the shared copy (with the %ANIMAL% token) is enough.
   ============================================================ */

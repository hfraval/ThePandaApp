# ``TPADiscovery``

The Discovery tab — host for the search entry point.

## Overview

`DiscoveryViewController` pins the Search bar (from `TPASearch`) at the top and shows discovery
content below. It's intentionally light today (an "under construction" placeholder body); its role
is to wire the search entry into the tab.
